server:
  servlet:
    context-path: /

spring:
  application:
    name: buytrip
  output:
    ansi:
      enabled: always
  main:
    banner-mode: off
 

logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: error


